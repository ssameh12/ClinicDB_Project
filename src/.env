# Copy this file to ".env" (no extension) and fill in your MySQL password.
# The app reads these values when it starts.

# Port the web app listens on. Open this in your browser: http://localhost:3001
PORT=3001

# MySQL connection details. Defaults below match a standard local MySQL install.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=P@ss02255
DB_NAME=ClinicDB
